export const deletePin = async () => {
    const deleteButton = document.getElementById("pop-up__delete");

    deleteButton.addEventListener("click", (e) => {
        const mapPinId = e.target();
    });
};
